import React, { Component } from "react";
import { hot } from "react-hot-loader";

class TicketsTable extends Component {
  render() {
    return 'TicketsTable';
  }
}

export default hot(module)(TicketsTable);
