import React, { Component } from "react";
import { hot } from "react-hot-loader";

class TicketUrlGenerator extends Component {
  render() {
    return 'TicketUrlGenerator';
  }
}

export default hot(module)(TicketUrlGenerator);
