import React, { Component } from "react";
import { hot } from "react-hot-loader";

class TicketIdInput extends Component {
  render() {
    return 'TicketIdInput';
  }
}

export default hot(module)(TicketIdInput);
